# Copy to .env and fill in values (never commit .env)

NODE_ENV=development
PORT=4000
LOG_LEVEL=debug

# Supabase project (Auth + PostgREST)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-or-service-role-key
