# Documentation

| Document | Description |
|----------|-------------|
| [PROJECT.md](./PROJECT.md) | **Main project / client delivery doc** — architecture, features, database, API, env vars, deployment checklist |
| [../backend/.env.example](../backend/.env.example) | Backend environment variables (copy to `backend/.env`) |
| [../web-app/lib/wk/config.ts](../web-app/lib/wk/config.ts) | Frontend `NEXT_PUBLIC_*` variables |

The repository root also links here from `CLIENT_PROJECT_DELIVERY.md` so there is a single canonical copy under `docs/`.
